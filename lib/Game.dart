class Game {
  String url;
  String title;
  int date;
  int month;
  int year;
  int completion;
  List<String> genres;

  Game(this.url, this.title, this.date, this.month, this.year, this.completion, this.genres);
}